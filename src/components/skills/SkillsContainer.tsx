import styles from "./SkillsContainer.module.css";

const SkillsContainer = () => {
  return (
    <div id="skills" className={styles.container}>SkillsContainer</div>
  )
}

export default SkillsContainer;
